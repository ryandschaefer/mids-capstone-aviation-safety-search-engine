---
apiVersion: v1
kind: Service
metadata:
  name: backend-bm25-service
  namespace: aviation-safety
  labels:
    app: backend-bm25-service
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
  selector:
    app: backend-bm25
