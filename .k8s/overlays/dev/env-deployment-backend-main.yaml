---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-main-deployment
spec:
  template:
    spec:
      containers:
        - name: backend-main-container
          env:
            - name: FRONTEND_URL
              value: "http://localhost"